{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build -w @bakery/shared && npm run build -w @bakery/api",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
